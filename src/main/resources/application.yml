spring:
  application:
    name:ai-code-backend

    # mysql
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/fox_ai_code
    username: root
    password: 123456

server:
  port: 8123
  servlet:
    context-path: /api
# springdoc-openapi com/carlos/aicodebackend
springdoc:
  group-configs:
    - group: "default"
      packages-to-scan: com.carlos.aicodebackend.controller
# knife4j
knife4j:
  enable: true
  setting:
    language: zh_cn
